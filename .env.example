# Database Configuration
DATABASE_URL=postgresql://cyberquiz:changeme@localhost:5432/cyberquiz
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cyberquiz
DB_USER=cyberquiz
DB_PASSWORD=changeme

# JWT Secret (change this to a random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Admin User (auto-created on startup)
ADMIN_EMAIL=admin@cyberquiz.fr
ADMIN_PASSWORD=password

# Node Environment
NODE_ENV=development

# Additional variables consumed by docker-compose and services
# Postgres image variables
POSTGRES_DB=cyberquiz
POSTGRES_USER=cyberquiz
POSTGRES_PASSWORD=changeme

# App convenience: a full DATABASE_URL (constructed from above values)
# Note: .env files don't support variable substitution - use explicit values
DATABASE_URL=postgresql://cyberquiz:changeme@localhost:5432/cyberquiz

# AI / embeddings
OLLAMA_BASE_URL=http://localhost:11434
QDRANT_URL=http://localhost:6333

# Allow external AI providers (true/false)
ALLOW_EXTERNAL_AI=false

